
on: 
  push:
    branches:
      - master



jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - uses:  actions/checkout@v3

      - name: "login to docker hub"
        run: docker login --username Abomera --password dckr_pat_NdPdQ0TzWyFUroureJ41ew1nVug

      # - name: "buliding docker image"
      #   run: docker bulid -t Abomera/myimage

      # - name: "pushing to docker hub"
      #   run: docker push Abomera/myimage